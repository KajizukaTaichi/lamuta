fun isPrime numToCheck =
begin
    let divisorList = [];
    for i in 2 ~ numToCheck do
    begin
        if numToCheck % i == 0 then
            divisorList += [i]
    end.
    divisorList != []
end.


let arr = [].
let idx = 0.

while let val = arr[idx] loop
begin
    print fizzbuzz val.
    let idx = idx + 1
end.
