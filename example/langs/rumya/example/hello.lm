let nameList = ["world", "Rumya"].
let index = 0.

while let name = nameList[index] loop
begin
    if let name = name as text then
    begin
        let greeting = "Hello, " + name + "! ".
        print greeting  * (index + 1)
    end.
    index += 1
end.
