let nameList = [["world", 1], ["Rumya", 4]].
let index: number = 0.

while let item = nameList[index] loop
begin
    if let [name, count] = item then
    begin
        let greeting = "Hello, " + name + "! ".
        print greeting  * count
    end.
    index += 1
end.
